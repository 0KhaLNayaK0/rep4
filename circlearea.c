#include <stdio.h>
int main()
{
    // This code is to calculate the area of a circle
    int a;
    float b=3.14;
    printf("Enter the radius of circle \n");
    scanf("%d" , &a );
    printf("The area of the circle is %f " , a*a*b );
    return 0;
}